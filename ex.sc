fn main (a, b, c)
{
    if (x > y)
    {
        y = sin(1);

        x = x + x + x * 2 * 3 * 1;

        x = 2 + 3;
    }

    
    x = x + 1;
    
    x = x + 1;
}

fn count (x, y, z)
{
    y = sin(5);
}
