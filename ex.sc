fn main ()
{
    input(x);

    input(y);

    input(z);

    if (x + 2 * y - 13 > y - 2 - 3)
    {
        y = sin (1);

        x = x + x + x * 2 * 3 * 1;

        x = 2 + 3;
    }
    
    x = x + 1;
    
    x = x + 1;

    ret x + 1;
}

fn count (x, y, z)
{
    y = sin(5);

    x = main();

    count(1, 2, 3);

    ret y;
}
